{
  "name": "greet",
  "version": "1.0.0",
  "description": "greeting function",
  "main": "index.js",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/Kipkemoii"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pub": "rm -rf dist && tsc && cp package.json .npmrc dist && cd dist && npm publish && cd ../ && rm -rf ../dist"
  },
  "author": "",
  "repository": {
    "url": "git://github.com/Kipkemoii/pkgs"
  },
  "license": "ISC"
}
